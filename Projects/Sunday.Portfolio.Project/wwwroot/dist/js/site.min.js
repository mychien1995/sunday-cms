!function(e){var t=document.getElementById("menu-toggle"),o=document.getElementById("menu"),i=document.getElementById("menu-close");t.addEventListener("click",function(e){o.classList.contains("open")?o.classList.remove("open"):o.classList.add("open")}),i.addEventListener("click",function(e){o.classList.remove("open")}),e(window).on("resize",function(){e(window).width()<846&&e(".main-menu a").on("click",function(){o.classList.remove("open")})}),e(".hover").mouseleave(function(){e(this).removeClass("hover")}),e(".isotope-wrapper").each(function(){var t=e(".isotope-box",this),o=e('input[type="radio"]',this),i=function(){var e=o.filter(":checked").data("type")||"*";"*"!==e&&(e='[data-type="'+e+'"]'),t.isotope({filter:e})};t.isotope({itemSelector:".isotope-item",layoutMode:"masonry"}),e(this).on("change",i),i()}),lightbox.option({resizeDuration:200,wrapAround:!0})}(jQuery),function(e){e(".main-menu li:first").addClass("active");e(".main-menu").on("click","a",function(t){var o,i,n,s;t.preventDefault(),o=e(this).attr("href"),i=!0,n=o.replace(/#/,""),s=e(".section").filter('[data-section="'+n+'"]').offset().top-0,i?e("body, html").animate({scrollTop:s},800):e("body, html").scrollTop(s)}),e(window).scroll(function(){e(".section").each(function(){var t=e(this),o=t.offset().top-80,i=o+t.height(),n=e(window).scrollTop();if(o<n&&i>n){var s=t.data("section");e("a").filter("[href*=\\#"+s+"]").closest("li").addClass("active").siblings().removeClass("active")}})}),e(".owl-carousel").owlCarousel({loop:!0,margin:30,nav:!1,responsive:{0:{items:3},600:{items:3},1000:{items:7}}})}(jQuery);
(function($) {
    var toggle = document.getElementById("menu-toggle");
    var menu = document.getElementById("menu");
    var close = document.getElementById("menu-close");

    toggle.addEventListener("click", function(e) {
        if (menu.classList.contains("open")) {
            menu.classList.remove("open");
        } else {
            menu.classList.add("open");
        }
    });

    close.addEventListener("click", function(e) {
        menu.classList.remove("open");
    });

    // Close menu after click on smaller screens
    $(window).on("resize", function() {
        if ($(window).width() < 846) {
            $(".main-menu a").on("click", function() {
                menu.classList.remove("open");
            });
        }
    });

    $(".hover").mouseleave(function() {
        $(this).removeClass("hover");
    });

    $(".isotope-wrapper").each(function() {
        var $isotope = $(".isotope-box", this);
        var $filterCheckboxes = $('input[type="radio"]', this);

        var filter = function() {
            var type = $filterCheckboxes.filter(":checked").data("type") || "*";
            if (type !== "*") {
                type = '[data-type="' + type + '"]';
            }
            $isotope.isotope({ filter: type });
        };

        $isotope.isotope({
            itemSelector: ".isotope-item",
            layoutMode: "masonry"
        });

        $(this).on("change", filter);
        filter();
    });

    lightbox.option({
        resizeDuration: 200,
        wrapAround: true
    });
})(jQuery);
(function($) {
    $(".main-menu li:first").addClass("active");

    var showSection = function showSection(section, isAnimate) {
        var direction = section.replace(/#/, ""),
            reqSection = $(".section").filter(
                '[data-section="' + direction + '"]'
            ),
            reqSectionPos = reqSection.offset().top - 0;

        if (isAnimate) {
            $("body, html").animate({
                    scrollTop: reqSectionPos
                },
                800
            );
        } else {
            $("body, html").scrollTop(reqSectionPos);
        }
    };

    var checkSection = function checkSection() {
        $(".section").each(function() {
            var $this = $(this),
                topEdge = $this.offset().top - 80,
                bottomEdge = topEdge + $this.height(),
                wScroll = $(window).scrollTop();
            if (topEdge < wScroll && bottomEdge > wScroll) {
                var currentId = $this.data("section"),
                    reqLink = $("a").filter("[href*=\\#" + currentId + "]");
                reqLink
                    .closest("li")
                    .addClass("active")
                    .siblings()
                    .removeClass("active");
            }
        });
    };

    $(".main-menu").on("click", "a", function(e) {
        e.preventDefault();
        showSection($(this).attr("href"), true);
    });

    $(window).scroll(function() {
        checkSection();
    });

    $('.owl-carousel').owlCarousel({
        loop: true,
        margin: 30,
        nav: false,
        responsive: {
            0: {
                items: 3
            },
            600: {
                items: 3
            },
            1000: {
                items: 7
            }
        }
    })
})(jQuery);